import { LinearProgress } from "@mui/material"
const HeaderProgress = () => (
    <LinearProgress
        sx={{ width: '100vw', position: 'fixed', top: '0rem', left: '0rem' }} />
)

export default HeaderProgress